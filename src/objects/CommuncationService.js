

// class for recieve messages from and send messages to a specific player
// should be generic enough to work through RTC or Socket
export default class {
  constructor() {

  }

  retrieveMessage(arg) {
    return Promise.resolve(arg);
  }

  sendMessage() {
    
  }
}